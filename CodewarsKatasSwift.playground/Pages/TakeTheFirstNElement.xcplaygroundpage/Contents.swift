func take(_ arr: [Int], _ n: Int) -> [Int] {
  let arr2 = arr.prefix(n)
  let arr3 = Array(arr2)
  return arr3
}


//Create a function take that takes an Array<Int> and an Int, n, and returns an Array<Int> containing the first up to n elements from the array.

